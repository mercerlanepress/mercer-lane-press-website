This folder demonstrates the expected names. Copy metadata.yml into a new folder
under book-intake/incoming/. Add exactly one manuscript (.pdf, .docx, .md, or
.txt) and exactly one original front cover (.jpg, .png, or .webp) to that folder.

The sample Amazon URL is deliberately not a real listing. Replace it with the
exact URL supplied by the owner before running the workflow.
